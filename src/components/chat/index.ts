/**
 * Chat 제품 컴포넌트 레이어의 공개 진입점(배럴).
 *
 * `src/components/prompt/**` 와 함께 Vapor primitive 를 사용할 수 있는
 * 제품 컴포넌트 레이어다. 앱 레이어는 이 배럴만 사용한다.
 */
export { ChatScreen } from './ChatScreen';
export type { ChatScreenProps } from './ChatScreen';
